# Deep Risk Process Manifest
# Minimal metadata - workflow.md is the source of truth

id: deep-risk
version: "1.0"
domain: risk-assessment
description: "Systematic Risk Assessment - theory-grounded, interaction-aware"

# Default Configuration for project workspace
default_config:
  paths:
    artifacts: "artifacts/"
    registers: "artifacts/risk-registers/"
    assessments: "artifacts/assessments/"

# File structure reference for the engine
paths:
  steps: steps/
