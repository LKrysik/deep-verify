# Deep Explore Process Manifest
# Minimal metadata - workflow.md is the source of truth

id: deep-explore
version: "2.1"
domain: decision-exploration
description: "Knowledge-First Decision Exploration"

# Default Configuration for project workspace
default_config:
  paths:
    artifacts: "artifacts/"
    research: "research/"
    reports: "reports/"

# File structure reference for the engine
paths:
  steps: steps/
