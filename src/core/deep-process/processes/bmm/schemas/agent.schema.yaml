# Agent Definition Schema
id: str
name: str
role: str
description: str
base_prompt: str
skills:
  - str # e.g. "python-expert", "security-auditor"
tools:
  - str # e.g. "read_file", "deep-verify"
mapping:
  claude_command: str # e.g. "/tech-lead"
  gemini_profile: str
